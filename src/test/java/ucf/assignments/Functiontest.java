package ucf.assignments;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class Functions {

    @Test
    void testRemoveTask() {
        /*assertequals(2, app.getList().size())
        *app.deleteTask(app.getList().size()), false);
        * assertequals(1, app.getList().size())*/


    }
    @Test
    void testcompleteTask(){
        /*assertEquals(1, app.getList().size());
         /*assertEquals(0, app.finishList().size());
        assertEquals(0, app.getListDone().size());
        app.markComplete(app.getList.size, true*/
         /*assertEquals(0, app.getList().size());
         /*assertEquals(1, app.getList().size());
          */

    }
    @Test
    void testAdddTask(){
       /* assertEquals(0, app.getList().size());*/
    }
}